[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
markers =
    visual: tests requiring Playwright + reference PNGs (auto-skip when unavailable)
    pdftools: tests requiring pdftotext/pdfinfo binaries (auto-skip when unavailable)
